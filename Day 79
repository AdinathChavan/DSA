class Solution {
    public int minDistance(String word1, String word2) {

       // int k =  solve(word1.length()-1,word2.length()-1, word1, word2);

        int m = word1.length();
        int n = word2.length();

        int[][] dp = new int[m+1][n+1];

        for(int i=0; i<=m; i++) dp[i][0]=0;
        for(int j=0; j<=n; j++) dp[0][j]=0;

        for(int i=1; i<=m; i++){
            for(int j=1; j<=n; j++){
                if(word1.charAt(i-1)==word2.charAt(j-1)){
                    dp[i][j]= 1+ dp[i-1][j-1];
                }
                else{
                    dp[i][j]= Math.max(dp[i-1][j], dp[i][j-1]);
                }
            }
        }
        
        return m+n-dp[m][n]*2;

        //return word1.length()+word2.length()- k*2;

    }

    // public int solve(int i, int j, String word1, String word2){

    //     if(i<0|| j<0){
    //         return 0;
    //     }

    //     if(word1.charAt(i)==word2.charAt(j)) return 1+ solve(i-1, j-1, word1, word2);

    //     else return Math.max(solve(i-1,j, word1, word2), solve(i, j-1, word1, word2));
    // }
}
